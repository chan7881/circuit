# circuit — 작업 규칙

중학교 「전기와 자기」 단원 수업용 시뮬레이터 모음. 학습지 앱(`chan7881/class`)에
iframe으로 임베드해서 쓴다.

## 규칙 1. 물리 검증 절차를 반드시 거친다 ★

물리 법칙으로 방향·부호·크기가 정해지는 것을 그릴 때는 **`docs/물리_검증_절차.md`의
7단계를 반드시 따른다.** 요약:

1. 이론값을 **시뮬 코드를 쓰지 않고** 따로 구한다(오른손 법칙, F = I×B 등)
2. 모델과 대조하고 차이를 **수치(°·배율)로** 남긴다
3. 차이가 **버그인지 오차인지** 판정한다 — 180°·90° 같은 딱 떨어지는 값은 버그다
4. 테스트를 추가하되 **방향(손잡이)까지** 못 박는다 — "수직인가"류는 부호 뒤집힘을 통과시킨다
5. **일부러 버그를 되돌린 복제본에서 그 테스트가 실패하는지** 확인한다(변이 검사)
6. **렌더링까지** 확인한다 — 예측을 먼저 세우고 스크린샷과 대조한다
7. 화면에 **그려 놓은 조건**(전류 화살표, N/S, +/−)과도 맞는지 본다

> 이 절차는 2026-08-07에 직선 도선 나침반이 **180° 반대**를 가리키던 버그를 잡으며
> 만들었다. 그 버그는 기존 테스트를 **통과하고 있었다** — 검증이 약하면 없는 것과 같다.

시뮬레이터는 학생이 관찰해서 법칙을 **스스로 찾아내는** 도구다. 화면이 물리와 어긋나면
그래픽 결함이 아니라 **틀린 법칙을 가르치는 것**이다.

## 규칙 2. 관찰 결과를 글자로 알려주지 않는다

안내 문구·주석·라벨은 **"무엇을 해 보라"까지만** 쓴다. "전류가 셀수록 자기장이 세진다"
같은 문장은 학생이 찾아내야 할 결론이므로 화면에 적지 않는다. 코일에 N/S를 미리
붙이지 않는 것도 같은 이유다(자석 극처럼 조건으로 주어지는 것은 표시해도 된다).

## 규칙 3. 구조

빌드 도구 없음. 순수 ES 모듈. 시뮬레이터마다:

```
<이름>/index.html      레이아웃 · 조작 UI
<이름>/js/model.js     순수 로직(DOM 모름) — 여기만 테스트한다
<이름>/js/render.js    그리기
<이름>/js/main.js      DOM 이벤트 ↔ 모델 ↔ 그리기 (document를 만지는 유일한 파일)
<이름>/js/tests.js     model.js 검증
<이름>/test.html       브라우저에서 여는 자체 검증 페이지
```

- 스타일은 `friction/styles.css` 하나를 모든 하위 시뮬레이터가 공유한다.
- 3D는 `vendor/three/`(three.js 0.160.0, MIT)를 import map으로 불러 쓴다. CDN 의존 없음.
- 모바일 우선: 탭 타깃 44px, `touch-action: none`, 납작한 임베드(높이 ~260px) 대응.

## 규칙 4. 커밋 전 확인

- 해당 `test.html`이 **전부 통과**하는지 (실패 개수 0)
- 바꾼 화면을 **실제로 눈으로** 확인했는지 (스크린샷)
- 물리를 건드렸다면 규칙 1의 절차를 거쳤는지

## 알아둘 것

- **브라우저가 ES 모듈을 캐시한다.** 고쳤는데 화면이 그대로면 포트를 바꿔서 열어 볼 것.
- 백그라운드 탭에서는 `requestAnimationFrame`이 돌지 않는다 → 그리기 함수를 직접 호출.
- GitHub Pages 배포는 커밋당 **10분쯤** 걸린다. 바로 안 보인다고 실패가 아니다.
- 자세한 내용은 `docs/물리_검증_절차.md` 부록 참고.
